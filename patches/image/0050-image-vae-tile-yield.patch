diff --git a/src/core/ggml_extend.hpp b/src/core/ggml_extend.hpp
index 1111111..2222222 100644
--- a/src/core/ggml_extend.hpp
+++ b/src/core/ggml_extend.hpp
@@ -7,6 +7,7 @@
 #include <stdarg.h>
 #include <algorithm>
 #include <atomic>
+#include <chrono>
 #include <cstdlib>
 #include <cstring>
 #include <fstream>
@@ -18,6 +19,7 @@
 #include <set>
 #include <sstream>
 #include <string>
+#include <thread>
 #include <unordered_map>
 #include <unordered_set>
 #include <vector>
@@ -935,6 +936,12 @@ __STATIC_INLINE__ sd::Tensor<float> process_tiles_2d(const sd::Tensor<float>& in
                 last_time  = (t2 - t1) / 1000.0f;
                 pretty_progress(tile_count, num_tiles, last_time);
             }
+            if (const char* value = getenv("TOSH_VAE_TILE_YIELD_MS")) {
+                const long delay_ms = std::clamp(strtol(value, nullptr, 10), 0L, 1000L);
+                if (delay_ms > 0) {
+                    std::this_thread::sleep_for(std::chrono::milliseconds(delay_ms));
+                }
+            }
             tile_count++;
         }
         last_x = false;
